<div class="fields">
    <div class="min_user">
      <!-- x icon to remove -->
      <%= f.link_to_remove("CANCEL") %>
      <!-- fields -->
      <%= f.input :name, :as => :check_boxes %>
    </div>
</div>